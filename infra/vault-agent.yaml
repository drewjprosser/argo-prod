apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-proxy
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/drewjprosser/argo-prod.git
    targetRevision: HEAD 
    path: infra/vault-agent
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true